// Assets/Scripts/UI/HudLayoutResetter.cs
using UnityEngine;

namespace SpaceGame.UI
{
    /// <summary>
    /// Optional: Hänge dieses Skript an ein leeres GameObject in deiner HUD-Szene
    /// und setze z. B. per Tastendruck alle gespeicherten Layouts zurück.
    /// </summary>
    public class HudLayoutResetter : MonoBehaviour
    {
        //[Tooltip("Optionaler Hotkey zum Zurücksetzen (z. B. F10).")]
        //public KeyCode resetKey = KeyCode.None;

        //private void Update()
        //{
        //    if (resetKey != KeyCode.None && Input.GetKeyDown(resetKey))
        //    {
        //        PlayerPrefs.DeleteAll();
        //        PlayerPrefs.Save();
        //        Debug.Log("[HUD] Alle gespeicherten Panel-Layouts wurden gelöscht. Starte die Szene neu.");
        //    }
        //}


    }
}
